# 安全边界

War Thunder UID Guard v0.7.2 对游戏采用“本地只读”设计。

## 强制限制

- HTTP 目标硬编码并校验为 `http://127.0.0.1:8111`。
- 仅允许 `/gamechat` 与 `/hudmsg` 两个只读端点。
- 禁用 HTTP 自动重定向，防止本地服务把请求导向其他地址。
- 禁用系统代理，确保请求不会经代理离开计算机。
- 不读取、捕获、OCR 或分析游戏渲染画面。
- 不访问 War Thunder 进程或进程内存。
- 不注入 DLL，不使用驱动、调试器或系统钩子。
- 不监听或模拟键盘、鼠标及其他游戏输入。
- 不读取或修改游戏安装目录、日志、回放及配置文件。
- 不提供自动瞄准、自动操作或其他游戏自动化功能。
- 不上传黑名单、事件或其他数据，不包含遥测。
- 专用同步服务器只提供公开黑名单，客户端仅接受内置 RSA-3072 公钥验签成功的数据；HTTP 传输内容被篡改时会被拒绝。
- 专用服务器保存的更新 ZIP 由已签名版本元数据绑定文件名、大小和 SHA-256；验签或校验失败时客户端会拒绝该文件并回退到 GitHub 正式发布源。
- GitHub Raw、Gcore、Fastly 和 jsDelivr 仅作为 HTTPS 后备来源；所有远程地址均执行精确主机、端口和路径限制。
- 更新检查优先访问本项目专用只读服务器，服务器元数据必须通过内置 RSA-3072 公钥验签，安装包必须同时通过签名元数据绑定的 SHA-256 校验；服务器不可用时才回退到本项目 GitHub 正式发布接口，并使用可回滚替换。
- 管理员上传不发送或保存明文密码；PBKDF2-SHA256 派生密钥用于 HMAC-SHA256 请求认证，并绑定时间戳、随机数、服务器基础哈希和内容哈希。服务器执行防重放、限流、格式校验、备份、原子替换和重新签名。

## 本地文件

程序唯一持久化的数据是用户维护的黑名单：

`%LOCALAPPDATA%\WarThunderUIDGuard\blacklist.json`

提醒通过 Windows 通知区域完成，不绘制游戏内覆盖层。

## 发布签名

当前发布版本没有 Authenticode 发布者签名，Windows 可能显示“未知发布者”。SHA-256 与服务器签名元数据用于验证文件完整性，但不能替代发布者身份签名；未来若重新启用可信代码签名，将按照 [CODE_SIGNING_POLICY.md](CODE_SIGNING_POLICY.md) 执行。

## 保证范围

以上内容是对本项目代码行为的技术保证，不代表 Gaijin Entertainment 或 BattlEye 的官方批准，也无法替代其最新规则。使用前请自行确认当前适用条款。
